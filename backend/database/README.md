# Backend Database Notes

- Use PostgreSQL for persistent entities: pilots, flights, aircraft, ranks, airports.
- Apply `../../database/schema.sql` to initialize tables.
- Add API keys for VATSIM/PTFS endpoints in environment variables.
